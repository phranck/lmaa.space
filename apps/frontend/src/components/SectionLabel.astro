---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<h2 class={`text-xs font-semibold uppercase tracking-widest text-stone-600 ${className}`}>
  <slot />
</h2>
