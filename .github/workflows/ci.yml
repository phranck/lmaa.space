name: CI

on:
  pull_request:

jobs:
  ci:
    name: Typecheck & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Typecheck (shared)
        run: bun run --filter=@lmaa/shared typecheck

      - name: Typecheck (backend)
        run: bun run --filter=@lmaa/backend typecheck

      - name: Typecheck (frontend)
        run: bun run --filter=@lmaa/frontend typecheck

      - name: Typecheck (dashboard)
        run: bun run --filter=@lmaa/dashboard typecheck

      - name: Build shared
        run: bun run build --filter=@lmaa/shared

      - name: Build backend
        run: bun run build --filter=@lmaa/backend

      - name: Build frontend
        run: bun run build --filter=@lmaa/frontend

      - name: Build dashboard
        run: bun run build --filter=@lmaa/dashboard
