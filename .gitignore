node_modules/
dist/
apps/backend/docs-dist/
apps/backend/openapi.generated.json
**/.astro/
**/package-lock.json
!/package-lock.json
.env
.env.local
*.db
*.db-shm
*.db-wal
.DS_Store
*.tmp
/.app/
/app
/app.config
/.grat/
/grat.config
/test-results/
.agents/
# The Graphify knowledge graph is rebuilt from the code on each machine, so the
# graph, the report, the manifests and the caches stay local.
graphify-out/
.claude/*
# The shop-check skill is what the automated review builds its prompt from, so
# that one skill is tracked and deployed with the backend.
!.claude/skills/
.claude/skills/*
!.claude/skills/lmaa-shop-check/
.codex/
.serena/
.pi/
memory/
papers/
plans/
scripts/*
!scripts/smoke-prod.sh
!scripts/check-csp.mjs
!scripts/check-web-fonts.mjs
!scripts/check-web-fonts.test.mjs
!scripts/check-analytics-integrity.mjs
!scripts/smoke-backend-bundle.mjs
!scripts/verify-deploy-artifacts.mjs
!scripts/install-git-hooks.mjs
# Git does not descend into an ignored directory, so scripts/lib has to be
# opened again before a single file inside it can be allowed through.
!scripts/lib/
scripts/lib/*
!scripts/lib/runtime-smoke.mjs
!scripts/hooks/
scripts/hooks/*
!scripts/hooks/pre-commit
apps/frontend/image-originals/
apps/backend/uploads/*
!apps/backend/uploads/.gitkeep
TO-DOS.md
WHATS-NEXT.md
shop-geo-data.json
/affiliate/
# Gource renders the repository history into a video. Its renderer, its avatars,
# its soundtrack and its configuration belong to one machine rather than to the
# project, and the soundtrack is licensed to a person rather than to this public
# repository.
.gource/
.gource.conf
mkgource
mkgource.command
# Local working directories of editor assistants, which belong to a machine
# rather than to the project. The instruction files beside them describe how one
# person works, so they stay local as well.
.continue/
.kiro/
.vibe/
AGENTS.md
CLAUDE.md

/skills
